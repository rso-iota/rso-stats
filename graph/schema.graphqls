# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Player {
  foodEaten: Int!
  deaths: Int!
  kills: Int!
}

type GlobalStats {
  foodEaten: Int!
  kills: Int!
}

type Query {
  player(id: ID!): Player
  players(ids: [ID]): [Player]
  stats: GlobalStats
}